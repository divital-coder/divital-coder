# julia          typescript           python                              rust    golang 
## (dash[plotly])  (svelte + sveltekit)  ( plotly + huggingface + tensorflow )

### Sentient Being, working every waking hour :P
